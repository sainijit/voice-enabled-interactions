"""Persistence layer: SQLite loyalty-profile repository + FAISS index manager.

Populated in Phase 3.  The repository keeps all SQL behind an abstract
interface; FAISS index files live on the identity-service volume only.
"""
