You are a senior UI developer and Senior UX Designer. The ux designs should be appealing as well as professional. 
We Need an new UI Screen in kiosk ui which runs on different port. 

New Screen should have below feature which are similar to existing UI. 
We are using actual kiosk for demo purpose where we are planning to show this screen. The existing ui will be shown on different Monitor. 

User will be doing interaction with this new ui which will be in kiosk.
There should be only on tag in screen Where Menu should be showown with queue count and it should change according to queue count. 
Each category in menu should have an small image of item for example all burger category will have one burger image you can use some library for these images or icons.
The cart should be shown 
there should be one ask button in the bottom
The UI should be responsive.

You can keep the code same for existing ui can use flag based approach which is passed as a varialbe in the container which shows new ui on new port. 

Can you understand the requireemtn and come up with a design 